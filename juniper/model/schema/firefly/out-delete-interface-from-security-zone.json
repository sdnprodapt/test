{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "delete-interface-from-security-zone out parameters schema",
    "type": "object",
    "properties": {
        "interface": {"type": "string", "description": "The name of the interface to remove from security zone"},
        "security_zone": {"type": "string", "description": "Name of the security zone that the interface is on", "default": "untrust"}
    },
    "required": ["interface"]
}