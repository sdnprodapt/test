{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "delete-interface out parameters schema",
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "The name of the interface to delete"},
        "security_zone": {"type": "string", "description": "The name of the security to the interface is on", "default": "untrust"}
    },
    "required": ["name"]
}