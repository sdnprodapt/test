{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "delete-interface out parameters schema",
    "type": "object",
    "properties": {
        "name": {"type": "string", "description": "The name of the interface to delete"},
        "security_zone": {"type": "string", "description": "The name of the security zone the interface is on"}
    },
    "required": ["name"]
}